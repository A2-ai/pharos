[package]
name = "scheduler"
version = "0.1.0"
edition = "2024"

[dependencies]
config = {path = "../config"}
nonmem = {path = "../nonmem"}
utils = {path = "../utils"}

anyhow = { workspace = true }
which = { workspace = true }
tera = { workspace = true }
fs-err = { workspace = true }
log = { workspace = true }

clap = { workspace = true, optional = true }

[features]
cli = ["dep:clap"]
