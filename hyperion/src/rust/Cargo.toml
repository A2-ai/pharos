[package]
name = 'hyperion'
publish = false
version = '0.1.0'
edition = '2024'
rust-version = '1.90'

[lib]
crate-type = [ 'staticlib' ]
name = 'hyperion'
path = 'lib.rs'

[workspace]
members = ["nonmem", "scheduler", "core"]

[dependencies]
# Internal crates
hyperion-nonmem = { path = "nonmem" }
hyperion-scheduler = { path = "scheduler" }
hyperion-core = { path = "core" }

# R Integration
extendr-api = { workspace = true }

# Pharos components (for init module)
config = { workspace = true }

# Core utilities
anyhow = { workspace = true }
fs-err = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }

[workspace.dependencies]
# R Integration
extendr-api = { version = '0.8.1', features = ["serde"] }

# Pharos components
nonmem = { git = "https://github.com/a2-ai/pharos", package = "nonmem", branch = "main" }
config = { git = "https://github.com/a2-ai/pharos", package = "config", branch = "main" }
scheduler = { git = "https://github.com/a2-ai/pharos", package = "scheduler", branch = "main" }

# Core utilities
anyhow = "1.0.100"
fs-err = "3.1.2"
serde = { version = "1.0.0", features = ["derive"] }
serde_json = "1"
toml = "0.9.7"

# Testing and utilities
insta = { version = "1.43.2", features = ['glob'] }
tempfile = "3.23.0"
zip = "6.0.0"

[profile.release]
lto = true
codegen-units = 1

[dev-dependencies]
tempfile = { workspace = true }
