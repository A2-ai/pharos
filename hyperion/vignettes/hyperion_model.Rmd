---
title: "Hyperion Model"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{copy_model}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(hyperion)
```

# Hyperion Model object

```{r}
mod <- read_model("test_data/models/onecmt/run002b001.mod")
mod

mod_nm <- read_model("test_data/mod/nmexample.mod")
print(mod_nm)

mod_e <- read_model("test_data/mod/everything.mod")
mod_e
```

```{r}
names(mod)

attributes(mod) |> names()
```

```{r}
read_model("test_data/models/onecmt/run001.mod") |>
  check_dataset("test_data/models/onecmt")

read_model("test_data/models/onecmt/run002.mod") |>
  check_dataset("test_data/models/onecmt")

read_model("test_data/models/onecmt/run003.mod") |>
  check_dataset("test_data/models/onecmt")
```


```{r}
check_model("test_data/models/onecmt/run001.mod")
```


```{r}
get_model_summary("test_data/models/onecmt/run002")
```

# Copy model

```{r}
copy_model(
  from = "test_data/models/onecmt/run002.mod",
  to = "test_data/models/onecmt/run003.mod",
  update = "all", #updates to run002 final estimates for theta + omega
  jitter = 0.3, #jitters run002 all initial estimates
  description = "Jittering initial estimates",
  overwrite = TRUE,
  seed = 804
)
```

```{r}
copy_model(
  from = "test_data/models/onecmt/run003.mod",
  to = "test_data/models/onecmt/run003b1.mod", #copies run003 to run003b1 with jittered parameters
  description = "Updating run003 to 003b1 with jittered params",
  jitter = 0.1,
  overwrite = TRUE,
  seed = 804
)
```


```{r}
copy_model(
  from = "test_data/models/onecmt/run002.mod",
  to = "test_data/models/onecmt/run002b001.mod",
  update = c("theta", "omega"),
  jitter = c(sigma = 0.3), #jitters run002 sigma initial estimates
  description = "Jittering initial sigma estimates, using theta/omega final estimates. Adding covariate",
  overwrite = TRUE,
  seed = 804
)
```

# Check Model

```{r}
example_tree <- get_model_lineage("test_data/models/onecmt/")

example_tree
```

