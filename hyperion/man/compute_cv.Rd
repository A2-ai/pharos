% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{compute_cv}
\alias{compute_cv}
\title{Compute coefficient of variation (CV\%) for random effect parameters}
\usage{
compute_cv(estimate, param_type, transform = identity)
}
\arguments{
\item{estimate}{The parameter estimate(s) (variance scale), can be a vector}

\item{param_type}{Parameter type(s), can be a vector: "Theta", "Omega", or "Sigma"}

\item{transform}{Transformation type(s), can be a vector: "LogNormal", "AddErr", "Proportional", or "Identity". Defaults to "Identity".}
}
\value{
CV as a percentage (vector), or NA if not applicable
}
\description{
Calculates the CV\% for Omega/Sigma diagonal parameters based on the
specified transformation. For LogNormal and AddErr transforms, uses
\code{sqrt(exp(estimate) - 1) * 100}. For Proportional, uses \code{sqrt(estimate) * 100}.
Returns NA for Theta parameters or Identity transform as CV is not meaningful.
}
\examples{
\dontrun{
compute_cv(0.09, "Omega", "LogNormal")
df \%>\% mutate(cv = compute_cv(estimate, kind, "LogNormal"))
}
}
