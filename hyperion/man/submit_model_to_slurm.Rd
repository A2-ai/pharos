% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{submit_model_to_slurm}
\alias{submit_model_to_slurm}
\title{Submits a NONMEM model to SLURM for execution}
\usage{
submit_model_to_slurm(
  model,
  overwrite = FALSE,
  dry_run = FALSE,
  run_in_output_dir = FALSE,
  ncpu = 1,
  partition = NULL,
  clean_level = 1,
  parafile = NULL,
  template = NULL,
  account = NULL
)
}
\arguments{
\item{model}{Path to the NONMEM model file, or character vector of model paths/patterns (required)}

\item{overwrite}{Whether to overwrite existing output files (default: FALSE)}

\item{dry_run}{Whether to perform a dry run without actually submitting the job (default: FALSE)}

\item{run_in_output_dir}{Whether to run the job in the output directory (default: FALSE)}

\item{ncpu}{Number of CPUs to allocate for the job (default: 1)}

\item{partition}{SLURM partition to submit the job to (default: NULL, uses cluster default)}

\item{clean_level}{Level of cleanup to perform after job completion (default: 1)}

\item{parafile}{Path to parameter file for parallel runs (default: NULL)}

\item{template}{Path to SLURM template file for job submission (default: NULL)}

\item{account}{SLURM account to charge the job to (default: NULL)}
}
\value{
Returns invisibly after printing job submission results. Prints model path and corresponding SLURM job ID for each submitted job.
}
\description{
This function submits a NONMEM model file to a SLURM cluster for execution,
allowing for parallel processing and job queue management. The function handles
job configuration, resource allocation, and job submission through pharos
}
\examples{
\dontrun{
# Submit a basic NONMEM model
submit_model_to_slurm("model.mod")

# Submit with custom job name and multiple CPUs
submit_model_to_slurm("model.mod", job_name = "my_analysis", ncpu = 4)

# Dry run to test submission without actually running
submit_model_to_slurm("model.mod", dry_run = TRUE)

# Submit to specific partition with account
submit_model_to_slurm("model.mod", partition = "gpu", account = "myproject")
}
}
